# Inhibitor validation record

`review.json` records actual opened screenshot hashes, observed defects, revisions,
and the real image-bearing model's responses. `benchmark.json` contains all poses
from the frozen three-seed 3VQU/O22 CPU recovery run, including the negative result,
seed sensitivity and symmetry-aware clusters. PNGs are actual browser captures.

The first image exposed heavy foreground occlusion. The model requested clipping;
the implementing agent also reduced trace weight and display hydrogen clutter.
The second image prompted a canonical O22 N2 / GLN670 OE1 distance check: 2.713622 Å.
The measurement supports proximity and does not assign hydrogen bonding.

Final comparison review exposed and corrected a missing reference ligand in the
prepared pane. Final surface review replaced visibly fragmented sphere patches
with a continuous approximate SAS, generated by marching tetrahedra at 0.6 Å.
Both paired panes use the same deposited coordinate frame and camera. The saved
comparison PNG precedes the caption correction that now identifies the deposited
reference rather than retaining the previously selected docking-score caption.

Inspected viewports: 1600×1000, 1920×1080 and 390×844. Captures use DPR 1, reduced
motion and Chromium/SwiftShader; final capture browser version was 151.0.7922.34.
The local runtime receipts additionally record frontend build and recipe hashes.
No optional dynamics frames or hardware-specific 50k-atom throughput are claimed.

The documented demo command was executed successfully, not just inspected as
source. A separate real cancellation check produced a cancelled terminal receipt
and no completed bundle. Two frozen-protocol runs yielded identical score/RMSD
rows. The implementation includes scoped identity/cursor, actor revision,
idempotency, invalid-protocol, canonical-distance and surface-coordinate tests.
