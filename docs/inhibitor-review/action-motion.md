# Inhibitor action motion review

[Play the recorded browser replay](action-motion.mp4).

Recorded from the real, isolated 3VQU/O22 experiment journal at 2× presentation speed, with every bundle read deliberately delayed by 1.5 seconds. The recording includes the actual 18 agent-authored operation receipts: docking, scene inspection, image observations, coordinate measurements and the unsuccessful recovery conclusion. No extra research actions or docking intermediates were invented for the movie.

The renderer continuously samples camera and annotation motion within each operation. Watch the changing camera approach, search-region scan, actual pose overlays and progressively constructed coordinate rulers. Result loading pauses the presentation clock. Molecular coordinates remain recorded outputs; this is inspection motion, not molecular dynamics. The numerical recovery conclusion remains negative.

The recording completed without page errors. Intermediate decoded frames were inspected. Automated browser checks separately exercise intermediate camera/ruler changes, pause and immediate user takeover. The movie shows the standalone workbench; the optional embedded component and shared node workspace are separate integration surfaces. This software-rendered example does not establish a 60-fps hardware performance claim.
