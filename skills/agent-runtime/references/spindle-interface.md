# Spindle experiments

Before the `spindle` action, request `get_skill` with name `spindle-interface` and wait for delivery. The action provides bounded native CPU jobs, numerical ensemble analysis and separate immutable scene/capture/vision records inside the current run. Full schemas and workflow are in that skill. It is an exploratory mechanics adapter, not an audited statistical method or biological confirmation.
