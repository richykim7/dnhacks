@AGENTS.md

MANDATORY: use the GitHub Board for every task. Before touching any file run `python3 scripts/board.py show`
and post a `claim` with `--files`. Post `update` on plan changes, `done` when work lands, and run
`python3 scripts/board.py check` before every commit. Never edit code without a claim. Full rules: `BOARD.md`.

<!-- Everything else shared with Codex lives in AGENTS.md; edit that, not this. -->
